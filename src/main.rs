use py2rs::text_chunker;
use py2rs::eval;

fn main() {
    // let document_uri = "books/book1.txt";
    // let text = text_chunker::read_text_file(document_uri, true).expect("Failed to read file");
    // let chunks = text_chunker::get_text_chunks(document_uri, &text, 5, &mut 0, true);
    
    // // Iterate over the first three chunks and print them
    // for chunk in chunks.iter() {
    //     println!("{}", "=".repeat(100));
    //     println!("{}", chunk);
    // }
    
}
